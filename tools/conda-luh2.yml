name: luh2
channels:
  - conda-forge
  - defaults
dependencies:
  - xesmf
  # xarray which is autodownloaded as xesmf dependency, uses scipy, which needs netcdf4 to open datasets
  - netcdf4
  - pynco
prefix: /home/glemieux/local/conda/miniconda3/envs/luh2
